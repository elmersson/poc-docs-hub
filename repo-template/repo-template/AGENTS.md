# <SERVICE-NAME>

See CLAUDE.md for full conventions. Build: `npm run build`. Test: `npm test`. Dev: `npm run dev` (port <PORT>).
Docs live in docs/ with required front-matter (owner, system, last_reviewed). Use the write-docs skill format.
